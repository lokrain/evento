// Placeholder for custom bindings options; expand when custom wiring is implemented.
export interface CarouselBindingsOptions {
  // TODO: define custom binding configuration (e.g., aria labels overrides, event strategy)
}